# SkillTest - 自动化测试靶场项目

[![Playwright Tests](https://img.shields.io/badge/Playwright-Tested-green)](https://playwright.dev/)
[![Node.js](https://img.shields.io/badge/Node.js-Express%20%2B%20EJS-blue)](https://nodejs.org/)

这是一个基于 Node.js (Express) + EJS 构建的前端 SSR 项目，旨在为 Web 自动化测试提供丰富的练习场景。项目集成了标准化的 **SKILLS 测试体系**，支持一键式自动化测试、报告生成及质量门禁。

## 🚀 快速开始

### 1. 安装依赖

```bash
npm install
```

### 2. 启动开发服务器

```bash
npm start
# 访问 http://localhost:3000
```

### 3. 运行自动化测试 (推荐)

一键执行全流程测试（服务启动 -> 冒烟 -> SSR -> E2E -> 异常测试 -> 报告）：

```bash
npm run skill:test
```

## 🎯 测试场景 (Target Practice)

本项目包含多种典型的 Web 交互场景，供测试脚本练习：

| 路径 | 描述 | 测试挑战点 |
| :--- | :--- | :--- |
| `/` | 首页 | 基础 SSR 渲染，Title/Meta 标签验证 |
| `/carousel` | 轮播图 (Swiper.js) | 复杂的组件状态、自动播放、类名变化检测 |
| `/form` | 表单交互 | 输入验证、异步 API 提交 (Fetch)、成功/失败消息捕获 |
| `/dynamic` | 动态内容 | DOM 增删操作、显式等待 (Explicit Wait)、JS 弹窗处理 |
| `/bad/html` | **HTML 结构异常** | 检测未闭合标签、嵌套错误、多余角标等 W3C 违规 |
| `/bad/logic` | **服务端逻辑异常** | 验证服务器错误处理 (500 Error)、防止进程崩溃 |

## 🛡️ SKILLS 自动化测试体系

本项目采用标准化的 `skills/` 目录结构，将测试逻辑与业务代码分离。

### 目录结构

```text
skills/frontend/
├── scripts/                  # 自动化辅助脚本 (Shell/Node)
│   ├── ssr-serve.sh          # 启动独立测试服务 (Port 3001)
│   ├── ssr-stop.sh           # 停止服务并清理端口
│   ├── ssr-routes-export.js  # 导出待测路由表
│   └── ssr-api-check.sh      # 接口冒烟测试 (Curl)
├── tests/                    # Playwright 测试用例
│   ├── ssr.spec.ts           # SSR 完整性测试 (No-JS 模式)
│   ├── e2e.spec.ts           # 关键业务流程交互测试
│   └── negative.spec.ts      # 异常场景与 HTML 规范测试
└── outputs/                  # 测试产物 (报告、日志、PID)
```

### 🛠️ 可用命令 (NPM Scripts)

| 命令 | 说明 |
| :--- | :--- |
| `npm run skill:test` | **全量测试**：按顺序执行以下所有步骤，失败即止。 |
| `npm run skill:serve` | 启动测试专用服务 (Port 3001) |
| `npm run skill:smoke` | 运行 API 冒烟测试 (快速检查连通性) |
| `npm run skill:ssr` | 运行 SSR 渲染测试 (Playwright No-JS) |
| `npm run skill:e2e` | 运行 E2E 交互测试 (表单、轮播等) |
| `npm run skill:negative` | 运行异常与规范测试 (html-validate, 500 checks) |
| `npm run skill:report` | **查看 HTML 测试报告** (在浏览器中打开) |
| `npm run skill:stop` | 停止测试服务并清理端口占用 |

## 📊 测试报告

测试完成后，Playwright 会在 `skills/frontend/outputs/report/html` 生成详细的 HTML 报告。

运行以下命令查看：

```bash
npm run skill:report
```

## 🧩 技术栈

- **后端**: Node.js, Express
- **模板引擎**: EJS
- **测试框架**: Playwright
- **验证工具**: html-validate, wait-on
- **前端库**: Swiper.js
