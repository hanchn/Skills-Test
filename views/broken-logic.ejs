<%- include('partials/header') %>

<div class="card">
    <h2>EJS 逻辑异常示例</h2>
    <p>这个页面在渲染时会抛出异常，因为引用了不存在的变量。</p>

    <!-- 引用不存在的变量 -->
    <p>欢迎你，<%= undefinedUser.name %></p>
</div>

<%- include('partials/footer') %>
